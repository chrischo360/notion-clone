{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Attatch by Process ID",
            // "skipFiles": ["<node_internals>/**"],
            // "program": "${workspaceFolder}\\server/src/index.ts",
            "processId": "${command:PickProcess}"
        }
    ]
}
