query GetBlock($blockId: Float!) {
  getBlock(blockId: $blockId) {
    id
    content
    type
    boldness
    order
    pageLink
    checked
    indentationLevel
    page {
      id
      title
    }
  }
}
