mutation createPage(
        $title: String!
        $emoji: String!
        $cover: String!
) {
    createPage(input: {title: $title, emoji: $emoji, cover: $cover}) {
        id
        title
        cover
        emoji
    }
}
