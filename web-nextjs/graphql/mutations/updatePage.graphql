mutation updatePage(
    $title: String!
    $cover: String!
    $emoji: String!
    $pageId: Float!
    $userId: Float!
) {
    updatePage(
        input: { title: $title, emoji: $emoji, cover: $cover, userId: $userId }
        pageId: $pageId
    )
}
